using UnityEngine;

public class PlayerIdleAnimationState : PlayerAnimationState
{
    public PlayerIdleAnimationState(PlayerAnimationController controller, Animator animator, int hashParameter) : base(controller, animator, hashParameter)
    {

    }
}
